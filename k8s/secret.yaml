# This is a template. In production, use an external secret manager
# (e.g., Vault, AWS Secrets Manager, sealed-secrets) instead of
# storing credentials directly in the repository.
#
# Generate base64 values:
#   echo -n 'your-password' | base64
apiVersion: v1
kind: Secret
metadata:
  name: pg-mentat-secrets
  namespace: pg-mentat
  labels:
    app.kubernetes.io/name: pg-mentat
type: Opaque
data:
  # Default values for development only. Override in production.
  postgres-password: bWVudGF0  # "mentat"
  postgres-user: bWVudGF0      # "mentat"
  postgres-db: bWVudGF0         # "mentat"
