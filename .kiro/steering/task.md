Hello Kiro.  This is a Rust project.  It's out of date and experiencing code rot.  Your task is to bring it up to date fixing issues along the way.  You'll need to upgrade all the package dependencies.  You'll apply known best practices in Rust programming.  You'll update the version of Rust used.  Along the way you'll test each step by working until you have a clean compile and error free test of the system.  Let's get started.
After running cargo clean and then building I see many warnings.  Please continue your work fixing these warnings, ensuring that packages are at their latest versions, running tests, etc. until everything (including cargo check) passes without errors.  Ensure that all builds and build options are exercised.

Is "#[allow(dead_code)]" considered good practice?  If not, can you avoid that?  Also, remember that you are not permitted to remove or change funcationlity or tests.  You are just updating software dependencies and fixing related errors to those changes.

